
# PocketPal AI — 未来 12 个月架构路线图 (月度版)

> 基线版本: **v1.15.2** (2026-07) | 无后端依赖 | 免费开源项目
>
> ## 🎯 项目北极星
>
> **把手机的本地推理做到极致。**
>
> PocketPal 不是"移动端的 LLM 客户端"，它就是**手机作为推理设备的终极形态**。
>
> - 一切在手机上跑。模型推理、TTS、STT、RAG、MCP 工具——全部在手机上完成。
> - 联网是为了增强搜索，不是为了上云。联网搜索是客户端直连搜索引擎，不经过任何中间服务器。
> - 数据在手机之间传递，不在服务器上停留。未来两台手机可通过热点/P2P 共享推理能力。
> - 导出/导入是数据主权方案，不是云同步的降级替代。你的数据你做主。
>
> **手机就是全部。不搞桌面端，不搞服务器。手机跑项目怎么不行了？**

> **约束**: 所有"联网"功能都是客户端直连（用户自带 API Key）或调用免费 API。不自建服务器。

---

## 🧭 架构总览（当前）

```
┌─────────────────────────────────────────────────────────────┐
│                    PocketPal App v1.15.2                     │
├──────────────┬─────────────┬────────────┬───────────────────┤
│  本地推理     │  客户端直连  │   离线工具  │   数据管理         │
│  (llama.rn)  │  OpenAI API │  MCP Tool  │  WatermelonDB     │
│  TTS 4引擎    │  HF 直连    │  Talent x6 │  导出/导入         │
├──────────────┴─────────────┴────────────┴───────────────────┤
│         UI Layer (60+ 组件 + Design Tokens + 14 Stores)     │
├─────────────────────────────────────────────────────────────┤
│              RN 0.82 + Native TurboModules + Hermes          │
└─────────────────────────────────────────────────────────────┘
```

**关键数据**: 851 源文件 | 239 测试 | 14 Stores | 7 表 | 6 Talent 引擎 | 零后端

---

## 🔵 7 月 — 数据导出/导入 + E2EE 备份

**版本**: v1.16.0

| 工作项 | 说明 | 架构影响 | 已有基础 |
|--------|------|----------|----------|
| 聊天记录 JSON 导出 | 所有会话 + 消息 → JSON 文件（带 `schemaVersion`） | `exportUtils.ts` | ✅ 已完成 |
| Pal 配置导出 | Pal 设置 + Prompt + 颜色 → JSON | 复用导出管线 | ✅ 已完成 |
| 加密导出 | E2EE (AES-GCM + PBKDF2) 加密后再写文件 | 复用 `E2EEService` + `exportEncryptedBackup` | ✅ 已完成 |
| 导入恢复 | 选择文件 → 自动识别明文/密文 → 解密 → 写入数据库 | `importUtils.restoreBackup` + `pickAndReadBackup` | ✅ 已完成 |
| 导出 UI | 设置页面"备份与恢复"卡片（导出/加密备份/导入） | SettingsScreen 新增卡片 + 密码弹窗 | ✅ 已完成 |
| 导出分享集成 | 系统分享菜单 (AirDrop/文件/邮件) | 复用 `react-native-share` | ✅ 依赖已安装 |

**关键原则**: 不搞云同步，不给用户画饼。导出 = 你的数据你做主。

---

## 🔵 8 月 — Android IAP + 全球支付

**版本**: v1.17.0

| 工作项 | 说明 | 架构影响 | 已有基础 |
|--------|------|----------|----------|
| Android IAP 端到端联调 | Google Play Billing 5/6 真机测试 | `AndroidIAPService` 已有框架 | ✅ 代码已有 |
| 非 US iOS IAP | 扩展 StoreKit 到全球区域 | `PaymentService` 已有 | 🟡 需配置 |
| 收据本地校验 | 客户端校验，无需服务端 | 新增本地验证逻辑 | 🔴 待开发 |
| IAP UI 适配 Android | Android 购买流程 UI 差异处理 | 复用 CheckoutFlowStore | 🟡 需适配 |

**注意**: Android IAP **不需要后端**。Google Play 处理所有支付和收据验证。

---

## 🔵 9 月 — 模型管理器重构 + 存储优化

**版本**: v1.18.0

| 工作项 | 说明 | 架构影响 | 已有基础 |
|--------|------|----------|----------|
| 模型下载队列 | 后台下载 + 断点续传 | `DownloadManager` + `DownloadQueueManager` | ✅ 代码已有 |
| 存储空间感知 | `getAvailableMemory()` + 自动告警 | `StorageOptimizer` 已有 | ✅ 代码已有 |
| 模型分类管理 | 底座/推理/TTS/VLM 分类 | `ModelManager` 已有 | ✅ 代码已有 |
| 存储缓存清理 | LRU + 未使用模型清理 | `StorageOptimizer` 扩展 | 🟡 需增强 |
| 技术债清理 | TypeScript 5.0.4 → 5.x | 全项目类型检查 | 🔴 待启动 |

---

## 🟠 10 月 — 语音输入 (STT) + 语音对话

**版本**: v1.19.0

| 工作项 | 说明 | 实现方式 |
|--------|------|---------|
| Whisper.cpp 集成 | 本地语音识别 | 通过 llama.rn 推理，完全离线 |
| 系统 Speech API 兜底 | 本地质量不足时的备选 | 调用 iOS/Android 原生 API |
| 语音输入按钮 | 录音 → 识别 → 填入消息框 | 新 UI 组件 |
| 全语音对话模式 | STT → LLM → TTS 闭环 | 无需打字，离线可用 |
| 语音引导设置 | 扩展现有引导流程 | 新增 voice 步骤 |

**后端依赖**: ❌ 零。Whisper 本地推理，系统 API 调用，都无需服务器。

---

## 🟠 11 月 — 联网搜索 + 内置工具集

**版本**: v1.20.0

| 工作项 | 说明 | 实现方式 |
|--------|------|---------|
| 联网搜索 Tool | 对话中输入关键词 → 返回搜索结果 | MCP Tool 实现，客户端直连 DuckDuckGo |
| 网页摘要 Tool | 输入 URL → 提取文本摘要 | 客户端直接 fetch，解析 HTML |
| 内置工具集整合 | 计算器 + 日期 + 提醒 + 换算 + 搜索 | 注册到 TalentRegistry |
| 隐私提示 UI | 用户首次使用联网功能时明确提示 | 确认后再允许联网 |
| 离线模式开关 | 全局"仅离线"模式，一键关闭所有联网 | Store 中新增 `offlineOnly` 标志 |

**关键设计**: 联网搜索作为 **MCP Tool** 实现，不是硬编码。复用 `AgentRunner` 管线。
用户不需要注册任何服务——DuckDuckGo 搜索无需 API Key。

**后端依赖**: ❌ 零。客户端直连搜索引擎 API。

---

## 🟠 12 月 — MCP 工具生态 + 工具权限

**版本**: v1.21.0

| 工作项 | 说明 | 架构影响 |
|--------|------|----------|
| MCP 协议标准化 | 定义 PocketPal Tool Plugin API | `ToolRegistry` 模块 |
| 更多内置工具 | 天气查询、新闻摘要 | 客户端直连免费 API |
| 工具权限系统 | 网络/文件/位置 → 用户授权 | `PermissionManager` 扩展 |
| 工具使用统计 | 展示各工具调用频率 | `TalentUIRegistry` 扩展 |
| 多模型并行实验 | 后台小模型常驻 | 纯本地 |

**后端依赖**: ❌ 零。

---

## 🟢 1 月 — 本地知识库 RAG (Alpha)

**版本**: v1.22.0

| 工作项 | 说明 | 实现方式 |
|--------|------|---------|
| 文档解析 | PDF/TXT/Markdown/网页 → 纯文本 | 本地解析，无需服务端 |
| 文本分块 | 语义分块 + 重叠窗口 | 纯本地 |
| 本地 Embedding | Nomic Embed Text (GGUF) | 通过 llama.rn 推理 |
| 向量存储 | SQLite + FTS5 混合索引 | 本地文件数据库 |
| 混合检索 | 语义 + BM25 → Re-rank → LLM | 全部本地完成 |

**后端依赖**: ❌ 零。全本地方案。

---

## 🟢 2 月 — 对话记忆系统升级

**版本**: v1.23.0

| 工作项 | 说明 | 架构影响 |
|--------|------|----------|
| 短期记忆重构 | 滑动窗口 + 摘要压缩 | `ShortTermMemory` |
| 工作记忆 | 跨 session 关键信息持久化 | `WorkingMemoryWriter` |
| 长期记忆 | 跨对话记忆图谱 | `MemoryGraph` 模块 |
| 实体提取 | 本地 NLP 提取关键事实 | 复用 Embedding 模型 |

**后端依赖**: ❌ 零。

---

## 🟢 3 月 — 多 Provider 远程推理

**版本**: v1.24.0

| 工作项 | 说明 | 实现方式 |
|--------|------|---------|
| Anthropic 适配器 | Claude API 接入 | 客户端直连，用户 Key |
| Gemini 适配器 | Google AI 接入 | 客户端直连，免费层可用 |
| Groq 适配器 | 免费高速推理 | 客户端直连，免费层可用 |
| Provider 配置 UI | API Key 管理 + 模型切换 | Settings 扩展 |
| 自动路由 | 本地/远程自动选择最优 | `RoutingEngine` 模块 |

**后端依赖**: ❌ 零。全部客户端直连，用户自带 API Key。

---

## 🟣 4 月 — 插件系统 (Alpha)

**版本**: v1.25.0

| 工作项 | 说明 | 架构影响 |
|--------|------|----------|
| 插件 Manifest 规范 | name / version / permissions | `PluginManifest` schema |
| Hermes 沙箱 | 独立 JS Bundle 隔离执行 | `PluginSandbox` |
| 插件生命周期 | 安装/启用/禁用/卸载 | `PluginManager` 状态机 |
| 插件 API | 消息钩子 / UI 注入 / 数据源 | `PluginAPI` |
| 分发机制 | 用户自行安装 `.pppak` 文件 | 无中央市场 |

**后端依赖**: ❌ 零。插件通过 GitHub Releases / 文件分享分发。

---

## 🟣 5 月 — 跨平台适配

**版本**: v1.26.0

| 工作项 | 说明 |
|--------|------|
| 大屏适配 | iPad / 平板 Split View |
| macOS 原生适配 | 菜单栏、快捷键、窗口管理 |
| 桌面快捷键 | Cmd+N / Cmd+W / 搜索 |
| 文件拖拽 | 拖拽文档触发导入 |

**后端依赖**: ❌ 零。

---

## 🟣 6 月 — 多模型协作流水线

**版本**: v1.27.0

| 工作项 | 说明 |
|--------|------|
| 模型流水线编排 | 小模型过滤 → 大模型分析 → TTS 输出 |
| 流水线配置 UI | 可视化编排 |
| 家庭 API 服务器 (可选) | 手机启动 HTTP，局域网调用 |
| 年度回顾 | 12 个月总结 + 下一年投票 |

**后端依赖**: ❌ 零。家庭 API 服务器是用户自己电脑开机时才跑的可选功能。

---

## 📊 月度里程碑一览

```
 7月   8月   9月   10月   11月   12月   1月    2月    3月    4月    5月    6月
┌────┬────┬────┬────┬────┬────┬────┬────┬────┬────┬────┬────┐
│数据 │Android│模型 │语音 │联网 │MCP  │RAG  │记忆 │多   │插件 │跨   │模型 │
│导出 │IAP  │管理 │输入 │搜索 │工具 │知识 │系统 │Prov │系统 │平台 │流水 │
│导入 │全球 │器   │STT  │内置 │权限 │库   │升级 │ider │     │适配 │线   │
│     │支付 │     │     │工具 │     │     │     │路由 │     │     │     │
├────┼────┼────┼────┼────┼────┼────┼────┼────┼────┼────┼────┤
│v1.16│v1.17│v1.18│v1.19│v1.20│v1.21│v1.22│v1.23│v1.24│v1.25│v1.26│v1.27│
└────┴────┴────┴────┴────┴────┴────┴────┴────┴────┴────┴────┘
  🔵 离线核心  │  🟠 语音+联网  │  🟢 智能增强  │  🟣 生态扩展
```

---

## 🌐 联网功能专项 — 所有联网都不需要后端

| 联网功能 | 实现方式 | 需要用户配置？ | 计划上线 |
|---------|---------|--------------|---------|
| Hugging Face 模型下载 | 客户端直连 HF API | ❌ 开箱即用 | ✅ 已有 |
| 远程 LLM API | 客户端直连 OpenAI API | ⚠️ 需要 API Key | ✅ 已有 |
| **联网搜索 ** | 客户端直连搜索引擎 | ❌ DuckDuckGo 免费 | v1.20 (11月) |
| **网页摘要** | 客户端直接 fetch URL | ❌ 开箱即用 | v1.20 (11月) |
| **实时天气/新闻** | 客户端直连免费 API | ⚠️ 可能需要 Key | v1.21 (12月) |
| **多 Provider 推理** | 客户端直连 API | ⚠️ 需要 API Key | v1.24 (3月) |
| STT 云端 Fallback | 调用系统原生 API | ❌ 开箱即用 | v1.19 (10月) |
| 数据导出/导入 | 本地文件系统 | ❌ 完全本地 | v1.16 (7月) |
| Android IAP | 客户端直连 Google Play | ❌ 开箱即用 | v1.17 (8月) |

---

## 📋 ADR 列表 (无后端版)

| ADR # | 决策 | 月份 | 核心取舍 |
|-------|------|------|---------|
| ADR-001 | 手动 JSON 导出/导入替代云同步 | 7月 | 方便性 vs 零成本 |
| ADR-002 | 纯客户端 IAP（无服务端校验） | 8月 | 功能完整度 vs 零运维 |
| ADR-003 | 本地 Whisper + 系统 API STT | 10月 | 离线可用 vs 识别质量 |
| ADR-004 | DuckDuckGo 客户端直连搜索 | 11月 | 零成本 vs 搜索精度 |
| ADR-005 | 全本地 RAG (SQLite + FTS5) | 1月 | 隐私 vs 大型文档性能 |
| ADR-006 | 纯客户端插件 (Hermes 沙箱) | 4月 | 去中心 vs 发现难 |

---

## ⚠️ 月度风险日历

| 月份 | 风险 | 概率 | 影响 | 应对 |
|------|------|------|------|------|
| 7月 | 导出格式设计不合理导致未来迁移困难 | 🟡 中 | 数据兼容性 | 提前定义 JSON Schema 版本号 |
| 8月 | Google Play 审核拒绝 IAP | 🟡 中 | Android 支付延期 | 提前沙箱测试 |
| 10月 | Whisper 移动端推理太慢 | 🟡 中 | STT 体验差 | 系统 API Fallback 兜底 |
| 11月 | DuckDuckGo 搜索质量不足 | 🟡 中 | 搜索结果差 | 提供可选的 Google/Bing API Key |
| 1月 | 低端设备 Embedding 过慢 | 🟡 中 | RAG 体验 | 按设备分级：低端仅 BM25 |
| 4月 | 插件沙箱安全漏洞 | 🔴 高 | 数据泄露 | 权限声明 + 沙箱加固 |

---

## 🎯 Phase 0 即刻行动 (2 周内)

1. **数据导出格式设计** — JSON Schema 定义 + 版本号
2. **Android IAP 真机联调** — 代码已有，验证购买流程
3. **联网搜索 POC** — DuckDuckGo 客户端直连可行性验证
4. **清理 Firebase 依赖为可选** — 确保不配置 .env 也能正常编译运行
5. **补充 ADR 文档** — 把零后端决策写进 ADR

> **核心哲学**: 没有后端，就把客户端做到极致。  
> 离线是默认，在线是增强。不加服务器。不欠运维债。
